# 今日热搜 · 技术设计文档（TECH_DESIGN.md）

> Day 5 初版 · 2026-09-20
> 依据：research.md（砍单）、PRD.md（功能与验收）、Day 5 拍板（默认路线 A）

---

## 一、技术路线对比与推荐

### 候选方案对比（3 套）

| 维度 | **A. 纯静态三件套** | B. Vite + React 工程 | C. CloudBase 全家桶 |
|---|---|---|---|
| 前端 | 手写 HTML/CSS/JS | React 组件化 + Vite 构建 | React + Vite + CloudBase SDK |
| 后端 | **无**（本期砍掉） | 无（仍是静态） | CloudBase Node.js 云函数 |
| 数据库 | **无**（data.js + localStorage） | 无 | CloudBase PostgreSQL |
| 部署 | 本地双击 / GitHub Pages | GitHub Pages / 静态托管 | CloudBase 静态网站托管 |
| 学习成本 | **零**（Day 1-4 已铺好的路） | 中（npm、组件、模块化） | 高（云函数、数据库、鉴权） |
| 适合本项目 MVP | **完全合身** | 杀鸡用牛刀 | 大幅超前 |

### 默认路线：**A. 纯静态三件套**

**推荐理由（取舍）**：

1. **功能匹配**：PRD 只有 3 个功能、2 个页面，A 全部装得下——F1 列表渲染、F2 详情展示、F3 收藏（localStorage）。
2. **成本匹配**：A 不用装任何新工具、不引入任何新概念，零基础可全程看懂每一行；B/C 的优势（组件复用、热更新、云端数据）对当前规模**全部用不上**。
3. **可迁移**：A 的文件结构是 B/C 的子集，升级时逐文件迁入，不浪费（见第八节迁移注意事项）。

**B/C 为什么本期不选**：B 的价值在页面多、复用多——2 个页面体现不出；C 的价值在真实数据与用户系统——PRD 已明确砍掉（research.md「本期不做」），等 Day 23+ 接真实 API 时再启动。

---

## 二、项目结构（路线 A）

```
vibe-coding-course/（项目根）
├── index.html        # F1 首页：分类导航 + 热搜列表（Day 7 实现）
├── detail.html       # F2 详情页：单条热搜 6 个展示字段（Day 7 实现）
├── style.css         # 两个页面共用的样式
├── data.js           # 演示数据：写死的热搜数组（Day 7 填充）
├── main.js           # 逻辑：读数据、渲染、收藏读写 localStorage
├── AGENTS.md         # 项目规则（已有）
├── research.md       # 需求研究（已有）
├── PRD.md            # 产品需求文档（已有）
└── TECH_DESIGN.md    # 本文档
```

> 说明：当前仓库里的 index.html 是 Day 2 的占位页，Day 7 开发 F1 时原位重写，不算「新建文件」。

---

## 三、数据模型

### 本期（data.js 中的演示数据）

每条热搜为一个对象，**7 个字段**（与 PRD 第 6 节一致）：

```js
{
  id: 1,                          // 唯一编号
  title: "示例热搜标题",           // 列表和详情都显示
  category: "科技",               // 分类：科技 / 娱乐 / 财经
  source: "微博",                 // 来源平台
  heat: 987654,                   // 热度数值（越大越靠前）
  summary: "一句话摘要，演示数据。",// 详情页展示
  link: "https://example.com"     // 跳转原文
}
```

**`collected`（收藏态）不是演示数据字段**——它是运行时状态，由用户点击产生，存 localStorage（见下）。

### 本期（localStorage 中的收藏数据）

- 键名：`favorites`，值为已收藏热搜的 `id` 数组，如 `[3, 17]`。
- 读：打开页面时读出，渲染列表/详情时对号入座显示收藏态。
- 写：用户点收藏/取消时更新数组并写回。

### 未来预留（CloudBase PostgreSQL，本期不实施）

表 `hot_items`（字段同上演示数据）；表 `favorites`（`user_id` + `item_id`，需登录体系配合）——Day 23+ 接真实数据时再细化。

---

## 四、API 列表

**本期没有 API**（无后端，全部数据来自 data.js）。

未来接真实数据时的预留设计（占位，不实施）：

| 接口 | 方法 | 作用 |
|---|---|---|
| `GET /api/hotlist?category=` | GET | 按分类返回热搜列表（替代 data.js） |
| `GET /api/hotlist/:id` | GET | 返回单条详情（替代 detail.html 直接读 data.js） |
| `POST /api/favorites` | POST | 同步收藏到云端（替代 localStorage） |
| `DELETE /api/favorites/:id` | DELETE | 取消云端收藏 |

---

## 五、前后端数据流（含数据流图）

### 本期数据流（纯前端）

```
┌─────────────────────────────────────────────────────────┐
│  用户的浏览器                                             │
│                                                          │
│  data.js（写死的演示数据）                                │
│     │  main.js 读取                                      │
│     ▼                                                    │
│  按分类筛选、按热度排序 ──► 渲染 index.html 列表（F1）      │
│     │                                                    │
│     ├─ 点某条 ──► detail.html 展示 6 个字段（F2）          │
│     │            └─ 点「返回」──► 回 index.html            │
│     │                                                    │
│     └─ 点收藏（F3）                                        │
│            │  main.js 更新                                │
│            ▼                                              │
│  localStorage["favorites"] = [3, 17]                      │
│     │  下次打开页面再读出来，恢复收藏态                      │
│     ▼                                                    │
│  界面上的收藏图标保持点亮                                   │
└─────────────────────────────────────────────────────────┘
```

**一句话版**：演示数据从 `data.js` 静态文件来 → 前端 JS 读取、按分类/热度整理后渲染成页面 → 用户收藏的数据存进浏览器 localStorage，下次打开再读出来。

### 未来扩展数据流（Day 23+，本期不实施）

```
各大平台热搜 API ──► CloudBase 云函数（定时抓取/聚合）
                        │
                        ▼
                  PostgreSQL 存储
                        │  用户浏览器请求（走云函数 API）
                        ▼
                  前端渲染（页面结构基本不变）
```

---

## 六、错误处理

对齐 PRD 第 7 节的 3 种异常状态：

| 异常 | 触发场景 | 用户看到什么 | 技术手段 |
|---|---|---|---|
| 数据为空 | data.js 数组为空或分类下无条目 | 「本分类暂无内容」提示，页面不空白 | 渲染前判空，走兜底文案 |
| 链接失效 | 演示数据的 link 打不开 | 浏览器自己的 404 页 | 演示数据保证链接可用；不额外处理 |
| localStorage 不可用 | 浏览器隐私模式/禁用存储 | 收藏功能提示不可用，浏览不受影响 | try/catch 包住读写，失败降级 |

---

## 七、环境变量

**本期没有环境变量**——纯静态、无密钥、无 API 地址。

- `.gitignore` 已包含 `.env` 系列规则（Day 2 落地并实测生效）。
- 未来接 CloudBase 时，`envId`、API 密钥等放 `.env`（本地）+ CloudBase 环境配置（云端），**永不进 git 提交**。

---

## 八、迁移注意事项（A → B/C 升级时）

### 方案变化时受影响的文件

| 若发生变化 | 受影响文件 | 说明 |
|---|---|---|
| 换用 React/Vite（A→B） | index.html、detail.html、main.js、style.css | 全部重写为组件；**data.js 原样保留**（数据结构不变） |
| 接真实 API（A→C 数据层） | data.js、main.js | data.js 被云函数替代；main.js 改为发请求；**两个 HTML 结构基本不动** |
| 收藏上云（A→C 用户层） | main.js（收藏相关部分） | localStorage 逻辑替换为调 API；**需先有登录**，UI 不变 |
| 加第 4 个分类 | 仅 data.js | 演示数据加条目即可，代码无需改（渲染逻辑是通用的） |
| 改字段（如加摘要图） | data.js + detail.html + main.js | 数据、展示、渲染三处同步改——**这正是 Day 4 字段数要统一的原因** |

### 迁移原则

1. **data.js 的 7 字段结构是锚**——三套路线共用，改它最贵，轻易不动。
2. 迁移顺序建议：先 B（前端工程化）或先 C（数据真实化）**二选一**，不要同时上。
3. 每次迁移前打 tag 或记 commit 号，出问题可精确回退（`git revert`）。
